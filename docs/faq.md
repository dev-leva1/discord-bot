# FAQ

## Почему конфиги переехали в data/?

Чтобы минимизировать корень и держать рабочие файлы вне кода.

## Где находится .env?

В `data/.env`. Пример в [docs/setup.md](setup.md:1).

## Можно ли запускать как раньше?

Да. Запуск сохраняется через `python bot.py` в корне.
